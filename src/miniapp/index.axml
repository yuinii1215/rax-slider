<swiper
  autoplay="{{autoPlay}}"
  indicator-dots="{{showsPagination}}"
  current="{{index}}"
  interval="{{autoPlayInterval}}"
  circular="{{true}}"
  onChange="onChange">
  <block>
    <slot />
  </block>
</swiper>
